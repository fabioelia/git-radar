{
  "name": "git-radar",
  "productName": "Git Radar",
  "version": "0.1.0",
  "description": "Sprint radar for your repositories. Tracks merged PRs through your release cycle with the GitHub CLI, organizes them into buckets of work using a local Gemma LLM, and reports how the sprint actually went.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20.11"
  },
  "packageManager": "pnpm@11.6.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "electron": "^36.0.0"
  }
}
